# 租车系统需求规格说明书

## 1. 简介

### 1.1 文档目的
本文档详细描述租车系统的功能需求、非功能需求、数据需求和用户界面需求，为系统开发提供明确的指导。

### 1.2 术语定义
| 术语 | 解释 |
|------|------|
| 车辆 | 可租赁的交通工具，包括轿车、SUV等类型 |
| 租赁 | 用户从系统中借用车辆并支付费用的行为 |
| 折扣策略 | 根据租赁天数计算折扣的规则 |
| 日租金 | 车辆每天的租赁费用 |
| 状态 | 车辆的当前状态，如可租赁、已租赁等 |

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 车辆管理
- **功能描述**：系统应能管理车辆信息，包括车辆ID、类型、型号、日租金和状态。
- **输入**：车辆信息（ID、类型、型号、日租金）。
- **输出**：车辆列表，显示所有车辆的详细信息。
- **流程**：系统初始化时加载预设车辆信息，用户可通过界面查看所有车辆。

#### 2.1.2 租车功能
- **功能描述**：用户可选择车辆进行租赁，输入租赁天数，系统计算租赁费用。
- **输入**：选择的车辆、租赁天数。
- **输出**：租赁费用详情，包括日租金、租赁天数、折扣率、总费用。
- **流程**：用户在主窗口选择车辆，点击"租车"按钮，在租车对话框中输入租赁天数，系统计算费用并显示费用详情。

#### 2.1.3 费用计算
- **功能描述**：系统根据车辆日租金、租赁天数和折扣策略计算总租赁费用。
- **输入**：车辆日租金、租赁天数。
- **输出**：计算后的总费用。
- **流程**：系统根据租赁天数应用相应的折扣策略，计算折扣后的总费用。

### 2.2 用户界面功能

#### 2.2.1 主窗口
- **功能描述**：显示车辆列表，提供"租车"和"刷新列表"按钮。
- **输入**：用户点击按钮操作。
- **输出**：车辆列表的显示和更新。
- **流程**：系统启动时显示主窗口，用户可查看车辆列表，点击"租车"按钮进入租车流程，点击"刷新列表"按钮更新车辆状态。

#### 2.2.2 租车对话框
- **功能描述**：用于输入租赁天数并计算费用。
- **输入**：租赁天数。
- **输出**：计算后的费用预览。
- **流程**：用户输入租赁天数，系统实时计算费用，用户确认后进入费用详情页面。

#### 2.2.3 费用详情对话框
- **功能描述**：显示租车费用的详细信息。
- **输入**：用户确认操作。
- **输出**：费用详情的显示。
- **流程**：系统显示费用详情，用户确认后完成租车流程。

## 3. 非功能需求

### 3.1 性能需求
- **响应时间**：系统界面操作响应时间不超过1秒。
- **处理能力**：系统应能处理至少100辆车辆的管理。

### 3.2 可用性需求
- **界面友好**：系统界面应简洁明了，操作流程直观。
- **错误处理**：系统应能处理用户输入错误，如非数字的租赁天数输入。

### 3.3 可靠性需求
- **数据一致性**：系统应保持车辆状态的一致性，避免同一车辆被重复租赁。

## 4. 数据需求

### 4.1 数据模型
- **车辆信息**：
  - 车辆ID（唯一标识符）
  - 类型（轿车、SUV等）
  - 型号（具体车型）
  - 日租金（每天的租赁费用）
  - 状态（可租赁、已租赁）

### 4.2 数据存储
- **存储方式**：内存存储，系统启动时初始化预设车辆数据。
- **数据初始化**：系统启动时自动加载预设的车辆信息。

## 5. 用户界面需求

### 5.1 界面设计原则
- **简洁明了**：界面布局清晰，操作流程简单直观。
- **响应及时**：用户操作后系统应及时响应并反馈。
- **一致性**：界面风格保持一致，按钮和控件的使用符合用户习惯。

### 5.2 界面组件
- **主窗口**：
  - 标题：租车管理系统
  - 车辆列表：使用表格显示车辆信息
  - 按钮：租车、刷新列表
- **租车对话框**：
  - 标题：租车
  - 输入框：租赁天数
  - 按钮：确认、取消
  - 费用预览：显示预估费用
- **费用详情对话框**：
  - 标题：费用详情
  - 信息显示：车辆信息、租赁天数、日租金、折扣率、总费用
  - 按钮：确认

### 5.3 界面流程图

```mermaid
graph TD
    A[主窗口] --> B[选择车辆]
    B --> C[点击租车按钮]
    C --> D[租车对话框]
    D --> E[输入租赁天数]
    E --> F[计算费用]
    F --> G[费用详情对话框]
    G --> H[确认租车]
    H --> I[更新车辆状态]
    I --> A
    A --> J[点击刷新列表]
    J --> A
```

## 6. 实现范围

### 6.1 包含的功能
- 车辆信息管理和显示
- 租车流程和费用计算
- 基于Swing的图形用户界面
- 折扣策略的应用

### 6.2 不包含的功能
- 数据库持久化存储
- 用户登录和权限管理
- 车辆预约功能
- 多用户并发操作

## 7. 验收标准

### 7.1 功能验收
- 系统能正确显示车辆列表
- 租车流程能正常完成
- 费用计算准确，符合折扣策略
- 车辆状态能正确更新

### 7.2 界面验收
- 界面布局合理，操作流畅
- 响应时间符合要求
- 错误处理机制有效

## 8. 风险分析

### 8.1 潜在风险
- 内存存储导致数据丢失
- 缺乏并发控制可能导致车辆状态不一致
- 界面响应时间可能受到车辆数量影响

### 8.2 缓解措施
- 定期保存数据到文件
- 实现简单的并发控制机制
- 优化界面渲染性能

## 9. 附录

### 9.1 车辆类型和日租金参考
| 车辆类型 | 型号 | 日租金（元） |
|----------|------|--------------|
| 轿车 | 经济型 | 100 |
| 轿车 | 舒适型 | 150 |
| 轿车 | 豪华型 | 200 |
| SUV | 紧凑型 | 180 |
| SUV | 中型 | 220 |
| SUV | 大型 | 280 |

### 9.2 折扣策略参考
| 租赁天数 | 折扣率 |
|----------|--------|
| 1-3天 | 无折扣 |
| 4-7天 | 9折 |
| 8-15天 | 8折 |
| 16天以上 | 7折 |